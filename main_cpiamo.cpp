
#include "vector.hpp"
#include <vector>
int		main(void)
{

	// int ints[] = {1, 2, 3, 4};
	ft::vector<int> vct;
	// ft::vector<int> tris(ints, ints+4);


	// tris.reserve(6);
	//tris.push_back(4);
// 	// ft::vector<int>::iterator it = (vct.end()-1);
	vct.insert(vct.begin(), 2, 21);
 	// vct.insert(vct.end(), 42);
	//  std::cout << vct[0] << std::endl;
	//  std::cout << vct[1] << std::endl;
	//  std::cout << vct[2] << std::endl;
	// tris.push_back(1);
	// tris.push_back(2);
	// tris.push_back(3);
	// tris.push_back(4);
	// tris.push_back(5);
	//tris.insert(tris.end(), 2, 0);



// 	// std::cout << *it;
// 	// vct.insert(vct.begin(), 2, 21);
// 	// for(size_t i = 0; i < vct.size(); i++)
// 	// 	std::cout << vct[i] << std::endl;
// std::cout << "size prima -> " << vct.size() << std::endl;
// std::cout << "cap prima -> " << vct.capacity() << std::endl;
// for(size_t i = 0; i < vct.size(); i++)
// 	std::cout<< vct[i] << std::endl;

// vct.resize(20);

// std::cout << "size dopo -> " << vct.size() << std::endl;
// std::cout << "cap dopo -> " << vct.capacity() << std::endl;

	// for(size_t i = 0; i < tris.size(); i++)
	// 	std::cout<< tris[i] << std::endl;



	//std::cout << *(vct.end() -1);
}



